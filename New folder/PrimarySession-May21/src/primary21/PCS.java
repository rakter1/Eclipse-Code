package primary21;

public class PCS implements ABC,BCD,DCA{

}


// One class can inherit only one class using extends keyword, This is the limitation of JAVA.
// One class can implements  more than one interface. 
// one interface can extends only more than one Interface, in this case we use extends keyword not implement.
// one interface can not implements another interface. 